-- LazyVim does not configure nvim-web-devicons directly; it is mocked
-- through mini.icons (see lua/config/plugins/mini-icons.lua). Require
-- the module here so the mock is installed before any consumer loads.
require("nvim-web-devicons")
